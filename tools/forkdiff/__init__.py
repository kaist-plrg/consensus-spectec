"""Fork-porting helper for the consensus SpecTec translation.

forkdiff diff <from-fork> <to-fork>   what changed upstream between two consensus-specs forks
forkdiff plan <diff.json> ...         copy the previous fork's SpecTec, stub what changed
"""
